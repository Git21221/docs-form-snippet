import React from 'react';

const SearchedTopics = ({subtopic}) => {
    console.log(subtopic);
    return (
        <div>
            subtopic
        </div>
    );
}

export default SearchedTopics;
